{
  "name": "image-search",
  "version": "1.0.0",
  "description": "",
  "main": "src/server/server.js",
  "scripts": {
    "start": "NODE_ENV=development nodemon build/server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir build -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bing.search": "^1.0.1",
    "dotenv": "^2.0.0",
    "express": "^4.13.4",
    "lodash": "^4.3.0",
    "moment": "^2.11.2",
    "mongoose": "4.4.1",
    "winston": "^2.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1"
  }
}
